import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class TopSecretTest {
    //just made a general TopSecret test file
    //unless we need other classes we can probably just add the rest of the tests to this one

    @Test
    void files() {
    }

    @Test
    void locateFile() {
    }
}